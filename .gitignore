node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.env.*
.envrc
.vscode/
*.tsbuildinfo

# Example files (not needed in repo)
samples/
docs/
test/
.cursorrules
WARP.md

# Local test configurations (exclude all, but keep example files only)
tests/local/*
!tests/local/*.example.json

# Test files (not needed in repo)
**/*.spec.ts
**/*.integration.test.ts
jest.config.js
coverage/
TESTING.md
INTEGRATION_QUICKSTART.md

# Reference repository (cloned n8n source for reference)
.reference/
